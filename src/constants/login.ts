interface LoginStrings {
  INVALID_CREDENTIALS: string;
  SIGN_IN: string;
}

const LOGIN_STRINGS: LoginStrings = {
  INVALID_CREDENTIALS: 'Invalid credentials.',
  SIGN_IN: 'Sign in',
};

export default LOGIN_STRINGS;
